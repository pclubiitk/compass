# Template version of secrets file for new devs to fill in

database:
  user: "postgres"
  password: "ritikaSQL"  # Same as above
  name: "campus_compass_final"
  host: "localhost"
  port: 5432

rabbitmq:
  user: "guest"
  password: "guest"  # Default credentials
  host: "localhost"
  port: 5672

jwt:
  secret: "test"

smtp:
  user: 
  pass: 