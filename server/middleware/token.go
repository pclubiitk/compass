package middleware

func GenerateToken() {
	// write the jwt token generator, modify the config and secret files as required
}

func VerifyToken() {
	// verify token function to extract the details and verify the token provided in the cookies
}